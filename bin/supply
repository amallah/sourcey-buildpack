#!/bin/bash
set -euo pipefail

BUILD_DIR=$1
CACHE_DIR=$2
DEPS_DIR=$3
DEPS_IDX=$4

export BUILDPACK_DIR=`dirname $(readlink -f ${BASH_SOURCE%/*})`
output_dir=$(mktemp -d -t supplyXXX)

echo "-----> Running sourcey"
pushd $BUILDPACK_DIR
popd